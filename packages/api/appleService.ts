// Mainly for login with google & apple functionality and possiby a logout that invalidates the google sesssion

// the callback succeeds, we will use coreService (in the handler on the auth screen) to send the token to backend for validation, login, or register
